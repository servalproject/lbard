#!/bin/csh -f

if ( -e testing ) then
  cd testing
endif

foreach instance ( serval? )
 # Stop relevant servald instance

 cd $instance
 setenv SERVALINSTANCE_PATH `pwd`
# ../../serval-dna/servald rhizome list
 ../../serval-dna/servald keyring list
 cd ..

end 
