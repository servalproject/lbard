#!/bin/csh -f

foreach bundle_count ( 1 2 4 8 16 32 64 128 )
  foreach prioritisation ( "" nopriority )
    screen -S experiment `pwd`/single_experiment_commands $bundle_count $prioritisation
  end
end
